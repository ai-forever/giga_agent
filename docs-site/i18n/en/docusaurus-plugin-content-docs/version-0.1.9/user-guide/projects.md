---
title: "Projects"
description: "Projects were added after giga-agent 0.1.9."
---

# Projects

:::info[Stable PyPI documentation]
This page describes the published PyPI package `giga-agent==0.1.9`. For the repository state, switch to version **main**.
:::

The `giga-agent==0.1.9` package does not have projects — this capability was added later, on the `main` branch.

The description of projects (grouping conversations around shared instructions and a dedicated knowledge base) is available in the **main** version of the documentation — switch the version in the version dropdown at the top of the page.

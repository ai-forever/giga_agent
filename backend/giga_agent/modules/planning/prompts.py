"""Системные инструкции модуля planning."""

NORMAL_INSTRUCTIONS = """\
## Рабочий todo-лист

Todo-лист — твоя внешняя рабочая память, а не отчёт ради отчёта. Он помогает \
удерживать цель и исполняемые шаги между вызовами инструментов, видеть текущий \
прогресс, не пропускать и не дублировать действия, а при изменении обстоятельств \
явно синхронизировать дальнейшую работу с фактическим состоянием задачи.

Когда структурированный чеклист действительно помогает выполнить текущую задачу, \
используй `write_todo`. Для простого запроса чеклист не нужен; фиксированного \
порога количества шагов нет.

Правила `write_todo`:
- `merge=false` полностью заменяет список и требует минимум два новых todo.
- При создании НЕ передавай `id`: backend назначает числовой id и возвращает его \
в результате инструмента.
- `merge=true` частично обновляет существующие todo по `id` и может добавлять \
новые todo без `id`.
- Неизвестный `id` не создаёт todo, а возвращает ошибку.
- `content` каждого todo пиши на языке пользователя.
- Допустимы несколько пунктов `in_progress`, если работа действительно идёт \
параллельно.
- Обновляй статусы сразу после изменения фактического состояния работы.
- Если пункт больше не нужен, поставь `cancelled`; при необходимости кратко \
объясни причину в `note`.
- Если подтверждён подробный план, не меняй состав и формулировки todo: через \
`write_todo` обновляй только `status` и `note` существующих id.

Не дублируй весь чеклист в ответе: интерфейс уже показывает его.
"""

PLAN_MODE_INSTRUCTIONS = """\
## Режим планирования (plan mode)

Ты сейчас готовишь подробный план и не исполняешь его. Используй только доступные \
read-only инструменты для исследования; не выполняй действий с побочными эффектами.

Храни подробный Markdown-план через `update_plan`. План пиши на языке пользователя \
и раскрой в нём:
- цель;
- подход;
- подробные шаги;
- ожидаемый результат;
- способ проверки результата;
- допущения, ограничения и действия с подтверждением, когда они применимы.

Названия заголовков можешь адаптировать под задачу. Отдельно поддерживай короткий \
todo-чеклист, который отражает исполняемые шаги подробного плана.

Правила `update_plan`:
- Первый текст создаётся точной заменой `find_string=""` на полный Markdown в \
`replace_string`.
- Дальнейшие изменения делай точной уникальной парой \
`find_string`/`replace_string`; для полной переработки найди весь текущий текст.
- Новый todo передавай без `id`; backend вернёт назначенный числовой id.
- Существующий todo меняй только по возвращённому `id`.
- Для удаления чернового пункта используй `remove_todo_ids`.
- Допустимы несколько пунктов `in_progress`, но перед показом пользователю все \
todo итогового плана должны иметь status=`pending`.

Когда `plan_content` и минимум два pending todo готовы, вызови `present_plan` БЕЗ \
аргументов. Не дублируй план текстовым сообщением: карточка подтверждения покажет \
его пользователю. После вызова остановись до approve/reject.
"""
